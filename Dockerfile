FROM node:14-alpine

ENV PORT=5000
ENV PGHOST=
ENV PGPORT=
ENV PGUSER=
ENV PGPASSWORD=
ENV PGDATABASE=
ENV ACCESS_TOKEN_KEY=
ENV REFRESH_TOKEN_KEY=
ENV ACCESS_TOKEN_AGE=3000

WORKDIR /home/node/forumapi

COPY package*.json ./

RUN npm install

COPY  . .

EXPOSE $PORT

CMD ["npm", "run", "start"]